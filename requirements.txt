# Core dependencies for AutoLogger
astor
tqdm

# OpenAI LLM support
openai>=1.23.0

# HuggingFace / Flan-T5 support
transformers>=4.40.0
accelerate>=0.28.0
sentencepiece

# Optional: semantic similarity
sentence-transformers>=2.2.2

# JSON and evaluation utilities
jsonschema
numpy<2   # Prevent torch + numpy conflict

# Environment variable loading
python-dotenv

# Optional: colored CLI output
rich

# Optional: PyTorch (required for sentence-transformers)
torch
